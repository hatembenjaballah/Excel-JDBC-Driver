# Excel-JDBC-Driver
Driver JDBC pour utiliser un fichier Excel comme base de données relationnelle (SQL complet, jointures, agrégations).  Transformez vos fichiers Excel en base de données SQL avec ce driver JDBC – compatible DBeaver.
